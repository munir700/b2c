<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- NOTE: order is important (the first matching state(s) is what is rendered) -->
    <item android:state_checked="true">
        <shape android:shape="oval">
            <stroke android:width="1px" android:color="?attr/colorPrimaryAttr" />
            <solid android:color="?attr/colorPrimaryAttr" />
            <size android:width="@dimen/_10sdp" android:height="@dimen/_10sdp" />
        </shape>
    </item>
    <item>
        <shape android:shape="oval">
            <stroke android:width="1px" android:color="@color/transparent" />
            <solid android:color="@color/transparent" />
            <size android:width="@dimen/_12sdp" android:height="@dimen/_10sdp" />
        </shape>
    </item>
</selector>