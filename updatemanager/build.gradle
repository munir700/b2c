apply plugin: 'kotlin-parcelize'
dependencies {
    project.updateManagerDependencies.each {
        add(it.configuration, it.dependency, it.options)
    }
    implementation 'com.google.android.play:app-update-ktx:2.0.0'
}
