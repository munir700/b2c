<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="name"
            type="String" />

        <variable
            name="contactId"
            type="String" />

        <import type="co.yap.yapcore.helpers.Utils" />

        <import type="android.view.View" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="@dimen/_8sdp"
        app:loadContactName="@{name}"
        app:loadContactPhoto="@{contactId}">

        <co.yap.widgets.CoreCircularImageView
            android:id="@+id/imgProfile"
            android:layout_width="@dimen/_45sdp"
            android:layout_height="@dimen/_45sdp"
            android:contentDescription="@string/empty_string"
            android:tag="imgProfile"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <LinearLayout
            android:id="@+id/lyNameInitials"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_round_with_space_white"
            android:tag="lyNameInitials"
            app:layout_constraintBottom_toBottomOf="@+id/imgProfile"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@+id/imgProfile">
            <!--            android:visibility="@{contactId.equals(``)? View.VISIBLE :View.GONE}"-->
            <TextView
                android:id="@+id/tvNameInitials"
                style="@style/ARegular"
                android:layout_width="@dimen/_45sdp"
                android:layout_height="@dimen/_45sdp"
                android:gravity="center"
                android:tag="tvNameInitials"
                tools:text="NM" />
            <!--            android:text="@{Utils.INSTANCE.shortName(name)}"-->
        </LinearLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>