<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="name"
            type="String" />

        <variable
            name="image"
            type="String" />

        <import type="co.yap.yapcore.helpers.Utils" />

        <import type="android.view.View" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="@dimen/_8sdp">

        <co.yap.widgets.CoreCircularImageView
            android:id="@+id/lyImage"
            android:layout_width="@dimen/_35sdp"
            android:layout_height="@dimen/_35sdp"
            android:contentDescription="@string/empty_string"
            app:categoryItem="@{viewModel.analyticsItem}"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@drawable/ic_shopping"
            app:position="@{viewModel.position}" />

        <LinearLayout
            android:id="@+id/lyNameInitials"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_round_with_space_white"
            app:layout_constraintBottom_toBottomOf="@+id/lyImage"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@+id/lyImage">

            <TextView
                android:id="@+id/tvNameInitials"
                style="@style/ARegular"
                android:layout_width="@dimen/_35sdp"
                android:layout_height="@dimen/_35sdp"
                android:gravity="center"
                android:text="@{Utils.INSTANCE.shortName(viewModel.analyticsItem.title)}"
                tools:text="NM" />
        </LinearLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>